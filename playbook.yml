- hosts: all
  collections:
    - arubanetworks.aoscx
  vars:
    ansible_python_interpreter: /usr/bin/python3
  gather_facts: False    
  tasks:
    - name: Convert text to binary
      shell: 'echo "Bienvenue" | xxd -b'
      register: binary_file
